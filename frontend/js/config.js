// js/config.js
// -----------------------------------------------------------------------
// Change these two values when you deploy the backend somewhere other
// than your local machine. Every other file reads from here, so this is
// the ONLY place you should need to edit for a new environment.
// -----------------------------------------------------------------------
const CONFIG = {
    API_BASE_URL: "http://localhost:4000/api",
    SOCKET_URL: "http://localhost:4000",
};
